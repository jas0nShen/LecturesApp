<view class="page">
  <view class="section panel">
    <view class="panel-title">毕业进度</view>
    <view class="big-number">{{audit.totalProgress}}%</view>
    <view class="progress-wrap">
      <view class="progress-bar" style="width: {{audit.totalProgress}}%;"></view>
    </view>
    <view class="subtitle">已记录 {{audit.completedCredits}} / {{audit.totalCreditRequired}} credits</view>
    <view class="source-line">数据源：{{dataSource === 'api' ? '本地 API' : '本地缓存'}}</view>
  </view>

  <view class="section panel">
    <view class="panel-title">录入已修课程</view>
    <view wx:for="{{courses}}" wx:key="id" class="course-row" data-id="{{item.id}}" bindtap="toggleCompleted">
      <view>
        <view class="course-code">{{item.courseCode}}</view>
        <view class="subtitle">{{item.titleEn}}</view>
      </view>
      <view class="check {{item.completed ? 'active' : ''}}">{{item.completed ? '已修' : '未修'}}</view>
    </view>
  </view>

  <view class="section">
    <view wx:for="{{audit.sections}}" wx:key="type" class="panel requirement">
      <view class="row">
        <view class="panel-title">{{item.name}}</view>
        <view class="muted">{{item.completedCredits}} / {{item.requiredCredits}}</view>
      </view>
      <view class="progress-wrap small">
        <view class="progress-bar" style="width: {{item.progress}}%;"></view>
      </view>
      <view wx:if="{{item.missingCourses.length}}" class="missing">
        <view class="missing-title">还缺</view>
        <view wx:for="{{item.missingCourses}}" wx:for-item="course" wx:key="id" class="missing-course">{{course.courseCode}} · {{course.titleEn}}</view>
      </view>
    </view>
  </view>

  <view class="section panel">
    <view class="panel-title">优先建议</view>
    <view wx:for="{{audit.recommendations}}" wx:key="course.id" class="recommendation">
      <view class="course-code">{{item.course.courseCode}}</view>
      <view class="subtitle">{{item.reason}}</view>
    </view>
    <view wx:if="{{audit.recommendations.length === 0}}" class="subtitle">当前示例规则下暂无缺口。</view>
  </view>
</view>
